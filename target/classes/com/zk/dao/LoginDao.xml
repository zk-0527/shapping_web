<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.zk.dao.LoginDao">
    <select id="LoginInfo" resultType="com.zk.pojo.User1">
       SELECT * FROM tuser WHERE username=#{username} and pwd=#{password}
    </select>

</mapper>